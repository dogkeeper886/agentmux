## Skills (`.claude/skills/`)

Skills define **what a capability is** and **when to use it**. They are reference documents — not invoked directly. They are the entry points that use commands.

## Commands (`.claude/commands/`)

Commands are user-invoked slash commands.
